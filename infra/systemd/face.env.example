# Common
ENV=prod
PORT=8080
API_KEY_SECRET=change-me
WORKER_API_KEY=change-worker-key
DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/postgres
JWT_ISSUER=face.local
JWT_AUDIENCE=face
# JWT_PRIVATE_KEY and JWT_PUBLIC_KEY can be provided as file paths or inline strings (base64 if needed)

# Worker
API_URL=http://127.0.0.1:8080
TENANT_ID=t-prod
SITE_ID=s-1
CAMERA_ID=c-1

